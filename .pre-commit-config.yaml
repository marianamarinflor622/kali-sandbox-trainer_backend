# Instalación: pip install pre-commit && pre-commit install
# Ejecutar manualmente en todo el repo: pre-commit run --all-files

repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.18.4
    hooks:
      - id: gitleaks
